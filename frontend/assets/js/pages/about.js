// About page — no interactive functionality required.
// This file exists to satisfy the tool-page convention
// (every pages/*.html has a corresponding pages/*.js).
document.addEventListener("DOMContentLoaded", () => {});
